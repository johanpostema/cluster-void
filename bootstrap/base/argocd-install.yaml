apiVersion: argoproj.io/v1alpha1
kind: ArgoCD
metadata:
  name: argocd
  namespace: argocd
spec:
  rbac:
    defaultPolicy: "role:admin"
  sourceNamespaces:
    - '*'
  server:
    route:
      enabled: true
  applicationSet:
    enabled: true
  dex:
    openShiftOAuth: true
