apiVersion: argoproj.io/v1alpha1
kind: ArgoCD
metadata:
  name: argocd
  namespace: argocd
spec:
  rbac:
    defaultPolicy: "role:admin"
  applicationSet: {}

  sourceNamespaces:
    - '*'
  server:
    extraArgs: []
    route:
      enabled: true

  controller:
    extraArgs: []
